# 忽略数据集 (太大了)
cs231n/datasets/
*.zip
*.tar.gz

# 忽略 Python 编译缓存
__pycache__/
*.py[cod]

# 忽略 Jupyter Notebook 的缓存检查点
.ipynb_checkpoints

# 忽略环境配置 (如果你把环境装在项目里的话)
env/
venv/
.env

# 忽略 VS Code 自己的配置
.vscode/
第三步：本地初始化与提交 (在 VS Code 终端里做)
在 VS Code 顶部菜单栏点击 Terminal (终端) -> New Terminal。
依次输入下面这些命令（每行按回车）：
code
Bash
# 1. 初始化 Git 仓库
git init

# 2. 检查 .gitignore 是否生效 (这步不是必须的，但建议看看)
# 这一步如果你没看到一大堆 dataset 文件，说明生效了

# 3. 把所有文件加入暂存区 (注意有个点)
git add .

# 4. 提交第一个版本
git commit -m "feat: init assignment1 environment"

# 5. 把主分支重命名为 main (现在的标准)
git branch -M main